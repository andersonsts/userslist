import { makeStyles } from '@material-ui/core/styles' 

export const useStyles = makeStyles(() => ({
  container: {
    paddingTop: '48px'
  },
  title: {
    fontSize: '54px',
    color: '#ffc124',
    textAlign: 'center',
    marginBottom: '16px'
  }
}))